<?php

return [

    /*
    |---------------------------------------------------------------------------
    | Окно редактирования / удаления сообщения
    |---------------------------------------------------------------------------
    |
    | Сколько минут после отправки автор сообщения может его править или
    | удалить. После окна — 403. Telegram-style: коротко по «горячим следам»,
    | без правки старых обращений задним числом.
    */
    'edit_window_minutes' => 10,

    /*
    |---------------------------------------------------------------------------
    | Вложения
    |---------------------------------------------------------------------------
    |
    | disk — приватный (НЕ public). Скачивание идёт через контроллер с явной
    | авторизацией (владелец тикета или админ). Mime-список — белый;
    | расширение валидируется Laravel'ом по сигнатуре файла.
    */
    'attachments' => [
        'disk' => env('SUPPORT_ATTACHMENTS_DISK', 'local'),
        'max_files_per_message' => 5,
        'max_file_size_kb' => 10240,
        'mimes' => ['jpg', 'jpeg', 'png', 'webp', 'gif', 'pdf', 'txt', 'doc', 'docx', 'xls', 'xlsx'],
    ],

];
